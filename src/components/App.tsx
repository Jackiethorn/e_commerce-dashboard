import { BrowserRouter } from "react-router-dom";
import Router from "../pages/router/router";

export const App = () => {

    return (
        <BrowserRouter>
            <Router />
        </BrowserRouter>
    )

}